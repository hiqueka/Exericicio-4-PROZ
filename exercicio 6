primos = []
todos = []
x=0
while x<=2:
    num = int(input("Digite um numero: "))
    x+=1
    if num > 1:
        for i in range(2, num):
            if (num % i) == 0:  
                break 
        else:
            primos.append(num)
print(primos)
